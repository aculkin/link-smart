import { auth } from './auth'
import { accounts } from './accounts'
import { images } from './images'
import { links } from './links'
import { users } from './users'

export const API = { auth, accounts, links, images, users }

export default API
