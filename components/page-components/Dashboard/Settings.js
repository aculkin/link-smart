import { EditAccountModal } from '../../generic-components/EditAccountModal'

export const Settings = ({ selectedAccount }) => {
  return <EditAccountModal account={selectedAccount} />
}

export default Settings
